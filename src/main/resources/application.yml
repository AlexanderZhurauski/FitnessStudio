spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/fitness
    username: postgres
    # password: ${DB_PASSWORD}
    password: secret

  mail:
    host: smtp.gmail.com
    username: gandalfdude@gmail.com
    # password: ${MAIL_PASSWORD}
    password: jffqvpctmgqqveya
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        smtps:
          trust: smtp.gmail.com


  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  servlet:
    context-path: /fitness

org:
  jobrunr:
    background-job-server:
      enabled: true
    dashboard:
      enabled: true
      port: 8000
    miscellaneous:
      allow-anonymous-data-usage: false
    jobs:
      retry-back-off-time-seed: 1
    database:
      table-prefix: app.

mail:
  confirmation:
    link: http://localhost:8080/fitness/api/v1/users/verification?code=%s&mail=%s
    subject: Activate your Fitness Studio Account
    text: "Dear valued user,\n\nThank you for choosing FitnessStudio, your ultimate fitness solution.
      We appreciate your decision to embark on this journey with us towards a healthier lifestyle.\n\n
      To proceed with your registration, please verify your email address by clicking on the following link - %s\n\n
      We look forward to being a part of your fitness journey.\n\nBest regards,\nThe FitnessStudio team"